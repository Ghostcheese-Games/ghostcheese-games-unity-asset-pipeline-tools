# schemas/

Canonical schemas and contracts used by shared tooling.

- `pipeline/` stores asset-pipeline schema definitions.
- `pipeline/shared-manifest.foundation.schema.json` defines the shared manifest/schema foundation (v1).
