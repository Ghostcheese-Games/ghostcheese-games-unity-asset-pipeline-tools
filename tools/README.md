# tools/

Home for reusable implementation code and tool entrypoints.

- `shared/` contains shared cross-pipeline primitives and helpers.
- `pipeline/` contains pipeline-focused tooling modules.
- `validation/` contains repository validation implementation. `validation/validate-repo-structure.sh` is the canonical validation entrypoint.
- `release/` contains release preparation automation scripts.
