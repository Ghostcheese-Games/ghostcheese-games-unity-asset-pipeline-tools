# tools/pipeline/ui_toolkit_graphics/

Unity UI Toolkit graphic asset package validator slice.

- `validate_package.py` validates package structure and manifest expectations against the shared manifest/schema foundation.

