# tests/unit/shared-manifest-schema/

Schema-foundation unit test artifacts for validator implementations.

- `cases.json` defines pass/fail cases against the shared foundation schema.
- `test_cases.py` executes the shared foundation fixture matrix and enforces expected pass/fail outcomes.
- Fixtures referenced by `cases.json` live under `fixtures/validation/shared-manifest/`.
