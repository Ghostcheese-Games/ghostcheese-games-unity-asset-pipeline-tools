# tests/

Automated tests for shared tooling behavior.

- `unit/` for focused behavior tests
- `integration/` for end-to-end pipeline/toolchain tests

Canonical repository validation/test entrypoint: `./tools/validation/validate-repo-structure.sh`.
